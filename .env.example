# Point the front end at a running operations API.
#
# Leave this unset (or delete the file) to run in self-contained demo mode,
# which needs no server — that is what the client demonstration uses.
VITE_API_URL=http://127.0.0.1:4000
